% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arrow.R
\name{a5_write_parquet}
\alias{a5_write_parquet}
\title{Write A5-keyed values to a Parquet file}
\usage{
a5_write_parquet(x, dest, compression = "zstd", ...)
}
\arguments{
\item{x}{Output of \code{\link[=a5_read_raster]{a5_read_raster()}} (tibble) or \code{\link[=a5_read_raster_arrow]{a5_read_raster_arrow()}}
(Arrow Table).}

\item{dest}{Output Parquet path.}

\item{compression}{Parquet compression codec. Default \code{"zstd"}.}

\item{...}{Further arguments forwarded to \code{\link[arrow:write_parquet]{arrow::write_parquet()}}.}
}
\value{
\code{dest} invisibly.
}
\description{
Convenience writer. Accepts either an \link[arrow:Table-class]{arrow::Table} from
\code{\link[=a5_read_raster_arrow]{a5_read_raster_arrow()}} or a \code{\link[tibble:tibble]{tibble::tibble()}} from \code{\link[=a5_read_raster]{a5_read_raster()}};
picks the right path under the hood. The on-disk schema is the same in
both cases when the tibble has a single fixed-length \code{value} list column
(i.e. was produced with \code{as_vector = TRUE}).
}
\examples{
\dontshow{if (requireNamespace("arrow", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\dontrun{
  tbl <- a5_read_raster_arrow(path, resolution = 14L, bands = 1:8)
  a5_write_parquet(tbl, "out.parquet")
}
\dontshow{\}) # examplesIf}
}
