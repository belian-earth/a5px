% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dequant.R
\name{dequant_aef}
\alias{dequant_aef}
\title{Dequantize Alpha Earth Foundations (AEF) embedding codes}
\usage{
dequant_aef(x)
}
\arguments{
\item{x}{Raw integer codes (numeric vector).}
}
\value{
The decoded values, same length as \code{x}.
}
\description{
The AEF int8 decode: \code{sign(x) * (x / 127.5)^2}. Per value, nonlinear and
sign-preserving, mapping the code range \verb{-127..127} to roughly \verb{[-1, 1]}.
Pass it as the \code{dequant} argument of \code{\link[=a5_read_raster]{a5_read_raster()}} (and the Arrow /
Parquet variants) so codes are decoded per pixel \emph{before} aggregation;
a nonlinear decode does not commute with the mean, so decoding aggregated
codes gives biased values.
}
\examples{
dequant_aef(c(-127, 0, 64, 127))
\dontrun{
  emb <- a5_read_raster(src, resolution = 14L, dequant = dequant_aef)
}
}
