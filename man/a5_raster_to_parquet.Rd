% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arrow.R
\name{a5_raster_to_parquet}
\alias{a5_raster_to_parquet}
\title{Read a raster and write it straight to Parquet (Rust-direct)}
\usage{
a5_raster_to_parquet(
  src,
  dest,
  resolution,
  stat = "mean",
  bands = NULL,
  bbox = NULL,
  src_nodata = NULL,
  as_vector = FALSE,
  value_type = c("float64", "float32"),
  compression = c("zstd", "snappy", "none"),
  cpu_workers = NULL,
  io_concurrency = NULL
)
}
\arguments{
\item{src, resolution, stat, bands, bbox, src_nodata, cpu_workers, io_concurrency, as_vector}{See
\code{\link[=a5_read_raster]{a5_read_raster()}}.}

\item{dest}{Output Parquet path.}

\item{value_type}{Storage type for the value column. \code{"float64"}
(default) or \code{"float32"} (halves the disk size for embeddings).}

\item{compression}{Parquet compression codec.
One of \code{"zstd"} (default), \code{"snappy"}, \code{"none"}.}
}
\value{
\code{dest} invisibly.
}
\description{
Same engine and on-disk schema as \code{\link[=a5_read_raster_arrow]{a5_read_raster_arrow()}} +
\code{\link[=a5_write_parquet]{a5_write_parquet()}}, but skips the R/Arrow round-trip entirely:
the Rust side aggregates pixels into A5 cells, builds the
\code{cell:uint64} + \verb{value:FixedSizeList<float, n_bands>} RecordBatch
from its existing flat buffer, and writes Parquet via the
\code{parquet} Rust crate. No intermediate R numeric materialisation.
}
\details{
Pick this when you want a Parquet file at the end and don't need
the Arrow Table in R. For million-cell embedding rasters it avoids
the per-cell list-of-vectors construction that the R-Arrow path
does, which is the main remaining cost in that pipeline.
}
\examples{
\dontrun{
  a5_raster_to_parquet(
    "https://data.source.coop/.../tile.tiff",
    "embeddings.parquet",
    resolution = 14L, bands = 1:8, value_type = "float32"
  )
}
}
