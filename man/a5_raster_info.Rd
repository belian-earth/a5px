% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/info.R
\name{a5_raster_info}
\alias{a5_raster_info}
\title{Inspect a raster's structure}
\usage{
a5_raster_info(src, store_opts = NULL)
}
\arguments{
\item{src}{Path or URL to a GeoTIFF / COG. Supported schemes: local path
(no scheme), \verb{file://}, \verb{http(s)://}, \verb{s3://}, \verb{gs://}, \verb{az://}. See
the "Remote sources" section of \link{a5px-package} for how cloud clients
are configured.}

\item{store_opts}{Named character vector or named list of object store
options for a remote \code{src}, applied after the environment defaults so
an explicit option always wins. Keys are the \code{object_store} config
keys for the provider, e.g. \code{aws_region}, \code{aws_skip_signature},
\code{aws_access_key_id}, \code{aws_secret_access_key}, \code{aws_session_token},
\code{aws_endpoint}, \code{aws_virtual_hosted_style_request}, the \verb{google_*} /
\verb{azure_*} equivalents, and for plain HTTP(S) the client keys such as
\code{timeout} or \code{allow_http}. Unknown keys, and any key given with a
local path, are errors. Inspect the result with \code{\link[=a5_store_config]{a5_store_config()}}.}
}
\value{
A list with elements:
\itemize{
\item \code{width}, \code{height}, \code{n_bands}: integer dimensions of the full
resolution image.
\item \code{dtype}: data type string, e.g. \code{"uint16"}, \code{"int8"}, \code{"float32"};
\code{"mixed"} when bands differ.
\item \code{nodata}: numeric nodata value, \code{NA} if the file declares none.
\item \code{band_names}: character vector from the GDAL \code{DESCRIPTION} tags, or
\code{band_NN} placeholders.
\item \code{interleave}: \code{"pixel"} or \code{"band"}; \code{compression}: codec name.
\item \code{block}: integer \code{c(width, height)} of the internal tiles;
\code{n_blocks}: integer \code{c(x, y)} tile counts. Zero for strip-based
files, which a5px cannot read.
\item \code{overviews}: data frame with one row per usable reduced-resolution
level (\code{level} = IFD index, \code{width}, \code{height}, \code{block_width},
\code{block_height}), in file order. Zero rows when there are none.
\item \code{crs}: \verb{EPSG:<code>} when the file carries an EPSG code, otherwise
the proj string a5px resolved.
\item \code{geotransform}: the 6-element GDAL geotransform.
\item \code{bbox}: WGS 84 envelope \code{c(xmin, ymin, xmax, ymax)} of the footprint.
}
}
\description{
Reads the metadata of a GeoTIFF / COG without touching pixel data:
dimensions, data type, nodata, band names, internal block (tile) grid,
overview levels, CRS and WGS 84 envelope. Use it to plan chunked reads
(see \code{bbox_align} in \code{\link[=a5_read_raster]{a5_read_raster()}}) or to check what a remote source
looks like before a long read.
}
\examples{
f <- system.file("extdata", "overview_cog.tif", package = "a5px")
info <- a5_raster_info(f)
info$block
info$overviews
}
