% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate.R
\name{a5_aggregate}
\alias{a5_aggregate}
\title{Aggregate A5-keyed values to a coarser resolution}
\usage{
a5_aggregate(
  x,
  to_resolution,
  ...,
  stat = c("mean", "sum", "count", "min", "max", "var", "sd")
)
}
\arguments{
\item{x}{A tibble or data.frame with exactly one column of class
\link[a5R:a5_cell]{a5R::a5_cell}.}

\item{to_resolution}{Integer scalar. The coarser target resolution.
Must be strictly less than the input cells' resolution.}

\item{...}{<\code{\link[tidyselect:language]{tidy-select}}> columns to aggregate.
Defaults to every non-cell numeric or list-of-numeric column.}

\item{stat}{Reducer. One of \code{"mean"} (default), \code{"sum"}, \code{"count"},
\code{"min"}, \code{"max"}, \code{"var"}, \code{"sd"}. \code{"count"} returns the number of
non-\code{NA} source values per group. \code{"var"} / \code{"sd"} use the sample
formula (divisor n - 1) to match R's \code{\link[stats:var]{stats::var()}} / \code{\link[stats:sd]{stats::sd()}};
single-row groups are returned as \code{NA}.}
}
\value{
A tibble keyed by parent cells at \code{to_resolution}, with the
selected columns reduced. The cell column retains its original name
and the \link[a5R:a5_cell]{a5R::a5_cell} class.
}
\description{
Lifts each row's A5 cell to its parent at \code{to_resolution} via
\code{\link[a5R:a5_cell_to_parent]{a5R::a5_cell_to_parent()}} (the A5-native centroid hierarchy) and reduces
the selected value columns within each parent group. Avoids re-reading
the source raster when you already have a high-resolution result and
want it summarised at a coarser scale.
}
\details{
Uses the centroid rule: each child cell is assigned to the parent whose
centroid contains it. This is exact for centroid placement but only
approximate for area, because A5 parent and child cells are not perfectly
nested. For embedding-style and distributional summaries this is a
non-issue; if you need conservative aggregation (counts that must sum
exactly), use a higher input resolution so boundary error is small
relative to cell area.

Both layouts produced by \code{\link[=a5_read_raster]{a5_read_raster()}} are supported:
\itemize{
\item wide: one numeric column per band (default).
\item list-of-numeric: a single fixed-length list column (\code{as_vector = TRUE}).
Reduced element-wise.
}
}
\examples{
\dontrun{
  fine <- a5_read_raster(path, resolution = 14L)
  coarse <- a5_aggregate(fine, to_resolution = 10L)

  # explicit column selection + sum reducer
  a5_aggregate(fine, 10L, B02, B03, stat = "sum")

  # list-column input (as_vector = TRUE) is reduced element-wise
  v <- a5_read_raster(path, resolution = 14L, as_vector = TRUE)
  a5_aggregate(v, 10L)
}
}
