% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{a5_read_raster}
\alias{a5_read_raster}
\title{Read a (Cloud-Optimised) GeoTIFF as A5-cell aggregated values}
\usage{
a5_read_raster(
  src,
  resolution,
  stat = "mean",
  bands = NULL,
  bbox = NULL,
  src_nodata = NULL,
  mode = c("forward", "overlay", "centroid"),
  subsamples = NULL,
  cpu_workers = NULL,
  io_concurrency = NULL,
  dequant = NULL,
  as_vector = FALSE,
  use_overviews = is.null(dequant)
)
}
\arguments{
\item{src}{Path or URL to a GeoTIFF / COG. Supported schemes: local path
(no scheme), \verb{file://}, \verb{http(s)://}, \verb{s3://}, \verb{gs://}, \verb{az://}.}

\item{resolution}{Integer scalar A5 resolution (0--30).}

\item{stat}{Aggregation. One of \code{"mean"}, \code{"sum"}, \code{"count"}, \code{"min"},
\code{"max"}, \code{"var"}, \code{"sd"}, \code{"majority"}, \code{"fractions"}, or any
non-duplicated subset of those (except \code{"fractions"}, which must be
alone) for a one-pass multi-stat read. Default \code{"mean"}. \code{"var"} /
\code{"sd"} use the sample formula (divisor n - 1) computed via Welford's
online algorithm in the streaming aggregator, matching \code{\link[stats:var]{stats::var()}} /
\code{\link[stats:sd]{stats::sd()}}; cells covered by a single pixel return \code{NA}.

\code{"majority"} and \code{"fractions"} are categorical: they treat the raw
integer codes as class labels, and require an integer source of 16
bits or fewer (like \code{dequant}, with which they cannot be combined).
\code{"majority"} returns the class with the greatest total weight in each
cell (pixel count under \code{mode = "forward"}, overlap area under
\code{mode = "overlay"}), ties broken toward the smallest class code.
\code{"fractions"} returns, per band, a list-column of named numeric
vectors: each cell's per-class share of its total valid weight
(shares sum to 1; names are the class codes). \code{"fractions"} is only
available in \code{a5_read_raster()} with \code{as_vector = FALSE}.}

\item{bands}{Bands to read. One of:
\itemize{
\item \code{NULL} (default): read every band.
\item integer / numeric vector: 1-based band indices to read.
\item character vector: band names matched against the GDAL \code{DESCRIPTION}
tag (falling back to \code{band_NN} when descriptions are absent).
}}

\item{bbox}{Optional spatial subset, as a numeric \code{c(xmin, ymin, xmax, ymax)} in WGS 84 lon/lat. When
supplied, only tiles overlapping the bbox (in raster CRS) are fetched
from the COG, and pixels outside the bbox are skipped. \code{NULL} (default)
reads the whole raster.}

\item{src_nodata}{Optional numeric scalar overriding the source nodata.
Use this when the file's \code{TIFFTAG_GDAL_NODATA} tag is missing or wrong;
it takes precedence over the metadata value when set. \code{NULL} (default)
uses whatever \code{async-tiff} exposes.}

\item{mode}{Sampling mode. \code{"forward"} (default) aggregates pixels into
cells by their centres; \code{"overlay"} aggregates with pixel-cell overlap
weights; \code{"centroid"} samples one pixel per cell at the cell centroid.
See Details.}

\item{subsamples}{Sub-point grid dimension per pixel for
\code{mode = "overlay"}: each pixel straddling a cell boundary is split
into \code{subsamples^2} sub-points whose per-cell counts give the overlap
weights (interior pixels take a fast path and never pay this cost).
\code{NULL} (default) auto-selects from the pixel/cell edge ratio so
sub-point spacing is at most half the cell edge, clamped to \verb{[2, 16]}.
Larger values approximate exact area weighting more closely. Only
used when \code{mode = "overlay"}; an error otherwise.}

\item{cpu_workers}{Number of CPU consumers in the tile-processing pool.
\code{NULL} (default) resolves from \code{getOption("a5px.cpu_workers")}, env
\code{A5PX_CPU_WORKERS}, then \code{\link[parallel:detectCores]{parallel::detectCores()}}.}

\item{io_concurrency}{Maximum in-flight tile fetches the producer issues
to the I/O stage. \code{NULL} (default) resolves from
\code{getOption("a5px.io_concurrency")}, env \code{A5PX_IO_CONCURRENCY}, then
\code{min(32, max(cpu_workers, 8))}. Bump this for cloud reads of multi-band
embedding rasters where the network can absorb more parallelism than
the CPU pool. See \code{\link[=a5px_set_concurrency]{a5px_set_concurrency()}}.}

\item{dequant}{Optional per-pixel decode applied \emph{before} aggregation: a
vectorised R function mapping raw integer codes to decoded values, e.g.
\link{dequant_aef} for Alpha Earth Foundations int8 embedding codes.
Nonlinear decodes do not commute with aggregation, so quantized values
must be decoded per pixel before the mean, not after; this argument
does exactly that.
The function is evaluated once over the full integer code domain to
build a lookup table applied on the Rust side, which restricts
\code{dequant} to sources with an integer data type of 16 bits or fewer
(int8 / uint8 / int16 / uint16); other dtypes error at read time.
nodata is matched against the raw code, before decoding. \code{NULL}
(default) reads raw values.}

\item{as_vector}{Logical. If \code{TRUE}, collapse the (selected) bands into
list columns of fixed-length numeric vectors -- one per stat. Single
stat returns a single column \code{value}; multi-stat returns
\verb{value_<stat>} columns (e.g. \code{value_mean}, \code{value_max}). Useful for
embedding rasters. Default \code{FALSE} (one numeric column per band).}

\item{use_overviews}{Logical. When \code{TRUE} and \code{stat = "mean"}, read the
coarsest COG overview that still oversamples the target A5 cell
instead of the full-resolution image. For aggregations to cells much
coarser than the source pixels this moves far fewer bytes and does far
less per-pixel work for a near-identical mean. Ignored unless the stat is
exactly \code{"mean"} (\code{sum}/\code{count}/\code{var}/\code{sd}/\code{min}/\code{max} are not preserved
under decimation) and in \code{mode = "centroid"}. Set \code{FALSE} to always read
full resolution. Requires the source to carry overviews; otherwise the
full-resolution image is read regardless. The default is \code{TRUE} unless
\code{dequant} is set: overviews are typically average-resampled, and an
overview pixel that is a mean of quantized codes decodes incorrectly.
Explicitly passing \code{TRUE} together with \code{dequant} warns and proceeds,
for sources whose overviews were built with nearest or mode resampling.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with columns:
\itemize{
\item \code{cell}: an \link[a5R:a5_cell]{a5R::a5_cell} vector at \code{resolution}
\item one numeric column per band (named after the band's GDAL \code{DESCRIPTION}
tag if present, else \code{band_01}, \code{band_02}, ...)
\item or, if \code{as_vector = TRUE}, a single list column \code{value} of length-N
numeric vectors (N = number of bands)
}
}
\description{
Streams a GeoTIFF or COG, projects each pixel centre to WGS 84, indexes it
to an A5 cell at the requested resolution, and aggregates per-band values
into a tibble keyed by cell.
}
\details{
The reader runs in Rust on top of \code{async-tiff} and \code{object_store}, never
loading the full raster into memory. CRS reprojection uses pure-Rust
\code{proj4rs}. Pixel sampling is forward (pixel-driven): each pixel contributes
its value to exactly one A5 cell, determined by the lon/lat of its centre.

Three sampling modes are available:

Multi-stat output naming: with \code{stat = c("mean", "max")} the wide
(per-band) tibble columns are \verb{<band>_mean}, \verb{<band>_max}. Single
underscore separator. Same convention applies in
\code{\link[=a5_read_raster_arrow]{a5_read_raster_arrow()}} and \code{\link[=a5_raster_to_parquet]{a5_raster_to_parquet()}} when
\code{as_vector = FALSE}.
\itemize{
\item \code{"forward"} (default): for each pixel, push its value into the cell
containing its centroid. Good when cells are larger than (or
comparable to) the pixels and you want a real aggregation. Equivalent
to GDAL's \verb{gdal raster zonal-stats --pixels=default}.
\item \code{"overlay"}: each pixel contributes to every cell it overlaps,
weighted by the overlapped fraction of its area (approximated by
sub-pixel supersampling; see \code{subsamples}). More accurate than
\code{"forward"} at cell/pixel boundaries, at some extra cost for pixels
that straddle a cell edge. Under overlay, \code{"mean"} is the
area-weighted mean, \code{"sum"} is mass-preserving (a pixel's value is
split across the cells covering it, so totals such as population
counts are conserved), and \code{"count"} is the effective (fractional)
number of contributing pixels. \code{"var"} / \code{"sd"} use frequency
weights with divisor \code{sum(w) - 1}.
\item \code{"centroid"}: for each cell intersecting the raster, sample the pixel
that contains the cell's centroid. Single value per cell, no
aggregation. Use this when the cell size is comparable to or smaller
than the pixel size — the forward path leaves gaps in that regime
because each pixel only contributes to one cell.
}

\strong{Scaling:} GDAL \code{Scale} / \code{Offset} tags are \emph{not} applied. Returned
values are in the raster's native domain. Apply scale/offset on the R side
if needed.

\strong{NoData:} the dataset-level GDAL \code{NODATA_VALUE} tag (when present) is
used to skip per-band samples. Per-band nodata via \code{GDAL_METADATA} XML is
not yet supported.
}
\examples{
\dontrun{
  path <- system.file("extdata", "example.tif", package = "a5px")
  tbl <- a5_read_raster(path, resolution = 14)
  tbl
}
}
