% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/store.R
\name{a5_store_config}
\alias{a5_store_config}
\title{Inspect the object store configuration a source resolves to}
\usage{
a5_store_config(src, store_opts = NULL)
}
\arguments{
\item{src}{Path or URL to a GeoTIFF / COG. Supported schemes: local path
(no scheme), \verb{file://}, \verb{http(s)://}, \verb{s3://}, \verb{gs://}, \verb{az://}. See
the "Remote sources" section of \link{a5px-package} for how cloud clients
are configured.}

\item{store_opts}{Named character vector or named list of object store
options for a remote \code{src}, applied after the environment defaults so
an explicit option always wins. Keys are the \code{object_store} config
keys for the provider, e.g. \code{aws_region}, \code{aws_skip_signature},
\code{aws_access_key_id}, \code{aws_secret_access_key}, \code{aws_session_token},
\code{aws_endpoint}, \code{aws_virtual_hosted_style_request}, the \verb{google_*} /
\verb{azure_*} equivalents, and for plain HTTP(S) the client keys such as
\code{timeout} or \code{allow_http}. Unknown keys, and any key given with a
local path, are errors. Inspect the result with \code{\link[=a5_store_config]{a5_store_config()}}.}
}
\value{
A named character vector. Always contains \code{provider} (one of
\code{"local"}, \code{"s3"}, \code{"gcs"}, \code{"azure"}, \code{"http"}); remote providers add
\code{host} and provider-specific fields such as \code{region}, \code{endpoint},
\code{skip_signature} and \code{static_credentials}.
}
\description{
Shows how a remote \code{src} will be accessed after environment variables
and \code{store_opts} are applied, without making a request. Use it to check
region, endpoint and signing before a long read. Credential values are
never returned, only whether static credentials are present.
}
\examples{
a5_store_config(
  "s3://us-west-2.opendata.source.coop/tge-labs/aef/v1/annual/2020/x.tiff",
  store_opts = c(aws_region = "us-west-2", aws_skip_signature = "true")
)
}
\seealso{
The "Remote sources" section of \link{a5px-package}.
}
