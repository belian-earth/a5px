% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arrow.R
\name{a5_read_raster_arrow}
\alias{a5_read_raster_arrow}
\title{Read a (Cloud-Optimised) GeoTIFF as A5 cells, returning an Arrow Table}
\usage{
a5_read_raster_arrow(
  src,
  resolution,
  stat = "mean",
  bands = NULL,
  bbox = NULL,
  src_nodata = NULL,
  mode = c("forward", "overlay"),
  subsamples = NULL,
  cpu_workers = NULL,
  io_concurrency = NULL,
  dequant = NULL,
  as_vector = FALSE,
  value_type = c("float64", "float32"),
  use_overviews = is.null(dequant)
)
}
\arguments{
\item{src, resolution, stat, bands, bbox, src_nodata, cpu_workers, io_concurrency, dequant, subsamples, as_vector, use_overviews}{See \code{\link[=a5_read_raster]{a5_read_raster()}}.}

\item{mode}{Sampling mode: \code{"forward"} (default) or \code{"overlay"}. The
\code{"centroid"} mode is only available in \code{\link[=a5_read_raster]{a5_read_raster()}}.}

\item{value_type}{Storage type for the value column. \code{"float64"} (default)
or \code{"float32"} (halves disk size for embeddings).}
}
\value{
An \link[arrow:Table]{arrow::Table} with two columns:
\itemize{
\item \code{cell}: \code{uint64} (interoperable with \code{\link[a5R:a5_cell_from_arrow]{a5R::a5_cell_from_arrow()}}).
\item \code{value}: \verb{FixedSizeList<float, n_bands>}. Field-level metadata records
the band names so dimension order is preserved when the table is
written to Parquet and read back in another language.
}
}
\description{
Same engine as \code{\link[=a5_read_raster]{a5_read_raster()}} but returns an Arrow \code{Table} whose value
column is a single \verb{FixedSizeList<float, n_bands>}. Bypasses R-list
construction entirely (Rust hands a flat cell-major buffer straight to the
Arrow array constructor), making it the right entry point for embedding
rasters destined for Parquet.
}
\examples{
\dontshow{if (requireNamespace("arrow", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\dontrun{
  tbl <- a5_read_raster_arrow(
    "https://data.source.coop/.../tile.tiff",
    resolution = 14L, bands = 1:8, value_type = "float32"
  )
  a5_write_parquet(tbl, "embeddings.parquet")
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=a5_write_parquet]{a5_write_parquet()}} to write the result to Parquet,
\code{\link[=a5_read_raster]{a5_read_raster()}} for the tibble path.
}
